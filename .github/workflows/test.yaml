name: Run HelloWorld Flow
on:
  push:

jobs:
  helloworld:
    permissions:
      id-token: write
      contents: read

    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.11

      - name: Install and Configure Outerbounds
        run: |-
          python -m pip install -U outerbounds && \
          outerbounds service-principal-configure \
          --name litellm-cicd \
          --deployment-domain dev-silverwing.outerbounds.xyz \
          --perimeter default \
          --github-actions

      - name: Run helloworld flow
        run: |-
          pwd
          cd flows/batch && python flow.py run --with kubernetes